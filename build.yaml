targets:
  $default:
    builders:
      auto_construction|auto_builder:
        enabled: true

builders:
  auto_builder:
    import: 'package:auto_construction/src/builder.dart'
    builder_factories: ['autoBuilder']
    build_extensions: { '.dart': [] }
    auto_apply: root_package
    build_to: source
