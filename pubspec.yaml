name: auto_construction
description: auto create from no parameters construction
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
  source_gen:

dev_dependencies:
  flutter_test:
    sdk: flutter